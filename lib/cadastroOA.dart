import 'package:flutter/material.dart';

class CadastroOA extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
        appBar: new AppBar(title: new Text("CADASTRO OA"),
        ),
        body: new Center(
          child: new Text("Em Manutenção"),
        )
    );
  }
}