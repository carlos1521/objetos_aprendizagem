import 'package:flutter/material.dart';

class Preferencias extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: new AppBar(title: new Text("Preferências"),
      ),
      body: new Center(
        child: new Text("Em Manutenção"),
      )
    );
  }
}